let msg: string = 'First message';

// Creating an element
let element = document.createElement('h1');
element.textContent = msg;

// Appending element to the document
document.body.appendChild(element);
